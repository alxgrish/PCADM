PCADM.Mobile
==============

Мобильная версия приложения на .NET MAUI.

Сборка:
    dotnet build PCADM.Mobile/PCADM.Mobile.csproj

Запуск (Android):
    dotnet build PCADM.Mobile/PCADM.Mobile.csproj -t:Run -f net10.0-android

Для работы потребуется сервер MySQL, доступный из устройства.
